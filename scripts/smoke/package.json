{
  "name": "demos-smoke",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Headless smoke test runner — boots every WASM demo to verify it survives module load + handler wiring.",
  "scripts": {
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
