-- MINIMAL STUB for WASM bisect — see /tmp/citizen.ail.orig for full version.
module cognitive_commons/services/citizen

import std/ai (callJson)
import std/result (Result, Ok, Err)
import cognitive_commons/types/personas (Persona)
import cognitive_commons/services/consensus (CommonsState)

export type AuthorReply = {
  text: string,
  glyph: string,
  manifesto: string
}

export func compose(
  persona_prompt: string,
  topic: string,
  recent_dialogue: string,
  proposer: Persona,
  st: CommonsState
) -> Result[AuthorReply, string] ! {AI} = {
  let _r = callJson(persona_prompt, "{}");
  Ok({ text: "stub", glyph: "", manifesto: "" })
}
